name: ${name}
description: ${description}
author: ${author}
main: ${mainClass}
version: ${version}
url: ${url}
commands:
  interfacemaker:
    aliases: ["im"]
    description: "InterfaceMaker is a revolutionary interface manager plugin."